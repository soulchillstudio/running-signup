請幫我改寫這份 HTML 報名頁，做「三班合一、Tab 切換」的整合。

## 現有檔案
我會附上目前的 HTML（檔名 2026-Q2-sign-up-page.html）。請保留現有的視覺風格、配色（--linen / --forest / --grass）、字體與區塊結構，只做以下改動。

## 改動目標
讓 **台中週二班、台北週三班、台中週四班** 三個班共用同一個報名入口，用戶用 Tab 切換查看該班的地點、上課日期，最後填一份表單即可。三班除了「地點」與「時段」不同，其他（價格、教練、課程內容、退費規則）完全相同。

## 具體改動

### 1. Hero 區：加上班級選擇 Tab
在 Hero 的「Location / Schedule / Price」資訊區**上方**加入一組班級切換 Tab：

```
[ 台中 · 週二班 ]  [ 台北 · 週三班 ]  [ 台中 · 週四班 ]
```

- 預設選中第一個（台中週二班）
- 點選 Tab 時，切換下方「地點」與「上課日期」的內容（JS 做切換）
- Tab 視覺要呼應現有設計（森林綠背景 + 選中用草綠 accent）

### 2. 新增「上課日期」區塊（取代或擴充現有 Schedule）
每個 Tab 對應一組 12 堂的日期清單：

**台中週二班**（中興大學田徑場）
5/26、6/2、6/9、6/16、6/23、6/30、7/7、7/14、7/21、7/28、8/4、8/11

**台北週三班**（台北田徑場）
5/27、6/3、6/10、6/17、6/24、7/1、7/8、7/15、7/22、7/29、8/5、8/12

**台中週四班**（中興大學田徑場）
5/28、6/4、6/11、6/18、6/25、7/2、7/9、7/16、7/23、7/30、8/6、8/13

日期以 12 格時間軸或小卡呈現皆可，風格簡約，不要占太大版面。

### 3. 報名表單：加入「班級」隱藏欄位 + 顯示選中班級
- 表單最上方顯示「你正在報名：台中週二班」這樣的確認條（會跟著 Tab 變）
- 表單送出時夾帶 `class` 欄位，值為 `taichung-tue` / `taipei-wed` / `taichung-thu`
- 匯款備註的提示文字也要跟著班級變（「請備註週二跑步班 / 週三跑步班 / 週四跑步班」）

### 4. Nav 的 CTA 要 scroll 到表單並保留當前 Tab 選擇

## 技術要求
- **單一 HTML 檔案**，所有 CSS/JS inline，不拆檔
- Tab 切換用**原生 JS**，不要引入任何框架
- Tab 內容用 `data-class` 屬性配對，用 `class="active"` 控制顯示
- 表單 `onsubmit` 先保留現有 demo handler，我之後會自己接 Google Sheet
- 保留所有現有區塊（Goal / Stages / Coaches / Gallery / FAQ / Footer）不動
- RWD：手機版 Tab 要能橫向捲動或堆疊，不能擠壓

## 輸出要求
**請直接輸出完整的單一 HTML 檔案（含改動後的所有內容），不要拆段說明、不要只給 diff。我要可以直接存檔覆蓋使用的版本。**
